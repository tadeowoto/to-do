doctype html
html
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Tareas del Usuario
    link(href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css", rel="stylesheet")
  body(class="bg-gray-900 text-white")
    .container(class="mx-auto px-4 py-12")
      h1(class="text-center text-4xl font-bold text-gray-100 mb-10") Tareas del Usuario

      .flex(class="flex-col md:flex-row gap-10 items-center justify-between")

        // Sección de Lista de Tareas
        .bg-gray-800(class="p-8 rounded-lg shadow-lg flex flex-col w-full md:w-2/3")
          h2(class="text-2xl font-semibold mb-6 text-gray-100") Lista de Tareas
          if tasks.length > 0
            ul(class="space-y-4")
              each task in tasks
                li(class="bg-gray-700 p-4 rounded-lg shadow-md")
                  h3(class="font-bold")= task.title
                  p= task.description
          else
            p(class="text-gray-400") No tienes tareas pendientes.

        // Sección de Agregar Tarea
        .bg-gray-800(class="p-8 rounded-lg shadow-lg flex flex-col w-full md:w-1/3")
          h2(class="text-2xl font-semibold mb-6 text-gray-100") Agregar Tarea
          form(action="/tasks" method="POST")
            .mb-4
              label(for="task-title" class="block text-sm font-medium mb-1") Título
              input(id="task-title" type="text" name="title" class="w-full px-3 py-2 bg-gray-700 text-white rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" required)
            .mb-4
              label(for="task-description" class="block text-sm font-medium mb-1") Descripción
              input(id="task-description" type="text" name="description" class="w-full px-3 py-2 bg-gray-700 text-white rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500")
            button(type="submit" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-lg") Agregar Tarea

    footer(class="text-center mt-10")
      p ©

